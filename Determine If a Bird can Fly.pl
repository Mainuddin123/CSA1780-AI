% Define facts about birds and whether they can fly.
can_fly(sparrow).
can_fly(eagle).
cannot_fly(penguin).

% Query whether a specific bird can fly.
% Example queries:
% ?- can_fly(sparrow).
% ?- cannot_fly(penguin).
